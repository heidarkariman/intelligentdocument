{% extends 'base.html.twig' %}

{% block title %}WorkType index{% endblock %}

{% block body %}
    <h1>WorkType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for work_type in work_types %}
            <tr>
                <td>{{ work_type.id }}</td>
                <td>{{ work_type.title }}</td>
                <td>
                    <a href="{{ path('app_work_type_show', {'id': work_type.id}) }}">show</a>
                    <a href="{{ path('app_work_type_edit', {'id': work_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_work_type_new') }}">Create new</a>
{% endblock %}
