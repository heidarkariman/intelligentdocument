{% extends 'base.html.twig' %}

{% block title %}FormType{% endblock %}

{% block body %}
    <h1>FormType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_type.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ form_type.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_form_type_index') }}">back to list</a>

    <a href="{{ path('app_form_type_edit', {'id': form_type.id}) }}">edit</a>

    {{ include('form_type/_delete_form.html.twig') }}
{% endblock %}
